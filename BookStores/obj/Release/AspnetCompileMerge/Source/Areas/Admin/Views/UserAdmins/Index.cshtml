@model IEnumerable<BookStores.Models.UserAdmin>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    int i = 0;
}

<div class="card" style="margin-top:75px;">
    <div class="card-body">
        <div class="row m-b-30">
            <div class="col-lg-8">
                <div class="d-md-flex">
                    <div class="m-b-10">

                    </div>
                </div>
            </div>
            <div class="col-lg-4 text-right">
                <a href="@Url.Action("Create", "UserAdmins")">
                    <button class="btn btn-primary btn-tone m-r-5">
                        <i class="anticon anticon-plus-circle m-r-5"></i>
                        Thêm mới Admin
                    </button>
                </a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-hover e-commerce-table">
                <thead>
                    <tr>

                        <th>STT</th>
                        <th>Tên Admin</th>
                        <th>Mã Admin</th>
                        <th>Số điện thoại</th>
                        <th>Tài khoản</th>
                        <th>Mật khẩu</th>
                        <th>Trạng thái hoạt động</th>
                        <th>#</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        i++;
                        <tr>

                            <td>
                                #@i
                            </td>
                            <td>
                                <div class="d-flex align-items-center">

                                    <h6 class="m-b-0">@item.nameAdmin</h6>
                                </div>
                            </td>
                            <td>@item.codeAdmin</td>
                            <td>@item.phone</td>
                            <td>@item.userName</td>
                            <td>@item.passWord</td>
                            <td>@item.phone</td>
                            <td>@item.access</td>
                            <td class="text-right">
                                <a href="@Url.Action("Edit","UserAdmins", new {id = item.idAdmin})">
                                    <button class="btn btn-icon btn-hover btn-sm btn-rounded pull-right">
                                        <i class="anticon anticon-edit"></i>
                                    </button>
                                </a>
                                <a href="/Admin/UserAdmins/Delete?id=@item.idAdmin">
                                    <button class="btn btn-icon btn-hover btn-sm btn-rounded">
                                        <i class="anticon anticon-delete"></i>
                                    </button>
                                </a>
                            </td>
                        </tr>
                    }


                </tbody>
            </table>
        </div>
    </div>
</div>